<tr>
  <th><%= post.id %></th>
  <th><%= post.date %></th>
  <th><%= post.rationale %></th>
  <th><%= post.user.full_name %></th>
  <td>
    <%= link_to 'Edit', edit_post_path(post), id: "edit_#{post.id}" %>
  </td>
</tr>
